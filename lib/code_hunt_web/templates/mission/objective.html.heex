<h1>Mission Objective: <span class="secondary type-out"><%= @mission.name %></span></h1>

<%= if DateTime.diff(@mission.details_release_date, DateTime.now!("Etc/UTC")) <= 0 do %>
  <p>This mission will end at <%= @mission.end_date |> DateTime.shift_zone("America/New_York") |> elem(1) |> Calendar.strftime("%m/%d/%y %-I:%M::%S %p") %>.</p>
  <h2>Details:</h2>
  <p><%= @mission.details %></p>
<% else %>
  <p>Details for this mission will be released at <%= @mission.details_release_date |> DateTime.shift_zone("America/New_York") |> elem(1) |> Calendar.strftime("%m/%d/%y %-I:%M::%S %p") %></p>
<% end %>

<%= link "Back", to: Routes.page_path(@conn, :index) %>
